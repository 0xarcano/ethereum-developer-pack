# Sesión 9 — Indexer de eventos

Esta carpeta contiene **solo** el código del hito de la sesión 9: contratos + frontend + `apps/indexer`.

## Setup

```bash
git checkout session-09
cd .. && pnpm install
export GOVERNOR_ADDRESS=0x...
pnpm --filter @mini-dao/indexer start
```

Detalle: [`apps/indexer/README.md`](./apps/indexer/README.md).
