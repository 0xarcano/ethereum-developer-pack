# Sesión 7 — Foundry

Esta carpeta contiene **solo** el código del hito de la sesión 7: mismos contratos + Foundry (build, script de deploy, smoke test).

## Setup

```bash
git checkout session-07
cd dapp/session-07/contracts
forge build
forge test
```

Deploy local: ver [`contracts/README.md`](./contracts/README.md).
